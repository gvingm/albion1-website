# Contributing to Альбион-1 Website

Спасибо за интерес к проекту! Мы приветствуем ваши вклады.

## Как внести вклад

### Сообщения об ошибках

1. Проверьте, не сообщалась ли ошибка ранее
2. Создайте issue с подробным описанием проблемы
3. Укажите шаги для воспроизведения
4. Добавьте скриншоты, если возможно

### Предложения по улучшению

1. Создайте issue с тегом `enhancement`
2. Опишите предлагаемое улучшение
3. Объясните, почему это будет полезно

### Код

1. Форкните репозиторий
2. Создайте ветку от `main`:
   ```bash
   git checkout -b feature/your-feature-name
   ```
3. Сделайте ваши изменения
4. Убедитесь, что проект собирается:
   ```bash
   pnpm build
   ```
5. Протестируйте локально:
   ```bash
   pnpm dev
   ```
6. Сделайте коммит с понятным сообщением:
   ```bash
   git commit -m "feat: добавить новую функцию"
   ```
7. Отправьте изменения и создайте pull request

## Стандарты кода

### Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование, отсутствующее влияние на код
- `refactor:` - рефакторинг кода
- `test:` - добавление или изменение тестов
- `chore:` - изменения вспомогательных инструментов

### TypeScript

- Используйте строгую типизацию
- Избегайте `any`
- Добавляйте комментарии к сложным участкам кода

### Компоненты React

- Используйте функциональные компоненты с хуками
- Следуйте принципам чистых компонентов
- Добавляйте PropTypes для проверки типов

### Стили

- Используйте TailwindCSS классы
- Следуйте mobile-first подходу
- Проверяйте на разных размерах экрана

## Разработка

### Установка зависимостей

```bash
pnpm install
```

### Запуск dev сервера

```bash
pnpm dev
```

### Сборка проекта

```bash
pnpm build
```

## Тестирование

Перед отправкой PR убедитесь, что:

1. Проект собирается без ошибок
2. Нет TypeScript ошибок
3. Дизайн корректен на мобильных устройствах
4. Все страницы доступны
5. Нет консольных ошибок

## Вопросы

Если у вас есть вопросы:

1. Проверьте существующие issues
2. Создайте новый issue с тегом `question`
3. Напишите на email: 560898@gmail.com

Спасибо за вклад в проект! 🙏